<h1>ID:<%= @task.id %>のタスク編集ページ</h1>

<%= form_with(model: @task, local: true) do |e|%>
  <%= e.label :content, 'タスク'%>
  <%= e.text_field :content%>
  <%= e.submit '送信' %>
  
<%end%>


<%= link_to '一覧に戻る', tasks_path %>